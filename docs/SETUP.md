# Accounts & secrets

Everything is created by the **designer** (her email, her card). The developer is invited as owner/admin on each. There is no handoff step later because nothing needs to move.

## Accounts

| Service | Plan | Why | Who's invited | Monthly |
|---|---|---|---|---|
| GitHub | Free org `<org>` | `studio` repo + one private repo per client | developer: owner | $0 |
| Vercel | **Pro** team (mandatory for client work) | hosting, one project per client + the intake app | developer: member | $20 + usage |
| Neon | **Launch** (no minimum) | one Postgres project per client + one `studio` project | developer: admin | pennies per quiet site |
| Cloudflare | Free | DNS zone for `<studio-domain>` + R2 bucket `studio-media` (custom domain `media.<studio-domain>`) | developer: admin | pennies (R2) |
| Resend | Free → $20 | magic links, notifications, "it's done" email; verify `<studio-domain>` | developer: member | $0–20 |
| npm | Free | scope `@<scope>` for `@<scope>/core` (public package, no secrets inside) | developer: member | $0 |
| Claude | **Max** | runs every build (`claude setup-token`), Claude Design, Claude Code on the web | — (developer uses their own) | plan price |
| Stripe | — | nothing at setup; each client connects their own account at go-live | — | $0 |

## Secrets and where they live

| Secret | Created in | Stored at | Used by |
|---|---|---|---|
| `CLAUDE_CODE_OAUTH_TOKEN` | designer's machine: `claude setup-token` (1-year, no refresh — calendar reminder) | GitHub secret on `studio` | build/fix-build steps only |
| `GH_PAT` | GitHub fine-grained PAT: org repo create, contents + actions on `studio` and client repos | GitHub secret on `studio`; Vercel env on intake | provision/ship; intake dispatch |
| `VERCEL_TOKEN`, `VERCEL_TEAM_ID` | Vercel team settings | GitHub secret; Vercel env on intake (console) | provision/ship; console |
| `NEON_API_KEY` | Neon org API keys | GitHub secret; Vercel env on intake (console) | provision/destroy |
| `CF_API_TOKEN`, `CF_ACCOUNT_ID`, `CF_ZONE_ID` | Cloudflare token scoped to DNS edit + R2 | GitHub secret; Vercel env on intake | provision (CNAME), presign |
| `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY` | Cloudflare R2 API tokens | GitHub secret; Vercel env on intake + every client project | presign, media URLs |
| `RESEND_API_KEY` | Resend | GitHub secret; Vercel env on intake + every client project | notify, magic links |
| `STUDIO_DATABASE_URL` | Neon `studio` project (pooled) | GitHub secret; Vercel env on intake | briefs/builds/invites |
| `AUTH_SECRET` (per client) | generated by provision | Vercel env on the client project | session cookies |
| `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` (per client) | client's Stripe account, restricted key | Vercel env on the client project (`set-stripe`) | donations |

### Non-secret configuration (GitHub *Variables* on `studio`, and the intake app's env)

`STUDIO_DOMAIN`, `MEDIA_BASE_URL` (`https://media.<studio-domain>`), `DESIGNER_EMAIL`, `EMAIL_FROM`, `VERCEL_TEAM_ID`, `NEON_ORG_ID`, `NEON_REGION` (e.g. `aws-us-west-2`), `CF_ZONE_ID`, `CF_ACCOUNT_ID`, `R2_BUCKET`, `MAX_TURNS` (from Phase-1 measurements). The intake app additionally has `INTAKE_URL` and `GH_ORG`.

Rules: secrets are declared per workflow **step**, never on the job — the model-running steps see only `CLAUDE_CODE_OAUTH_TOKEN` and the client `DATABASE_URL`. No secret is ever copied into a client repo.

## Learned the hard way (2026-08-30 first real run)

- **Vercel Hobby only deploys commits authored by the connected GitHub account.** The pipeline commits as `<GH_ORG>@users.noreply.github.com` (set `GIT_AUTHOR_EMAIL` to override). Anything else shows as `BLOCKED`.
- **R2 bucket needs a CORS policy** or browser uploads (intake photos, admin image picker) fail silently. Dashboard → R2 → bucket → Settings → CORS policy:
  `[{"AllowedOrigins":["https://*.<studio-domain>","http://localhost:3000","http://localhost:3200"],"AllowedMethods":["GET","PUT","HEAD"],"AllowedHeaders":["*"],"ExposeHeaders":["ETag"],"MaxAgeSeconds":3600}]`
  (Object Read & Write API tokens cannot set this; it's a one-time dashboard step.)
- **R2 custom domain** (`media.<studio-domain>`) is added from the bucket's Settings → Custom Domains; it creates its own DNS record.
- **Resend sending domain**: use a subdomain (`studio.<domain>`) so it never collides with another Resend account holding the apex; the pipeline adds DKIM/SPF/DMARC records via the Cloudflare DNS token.
- **A new subdomain takes ~1–2 min to get a certificate**; `ship` waits for the host before smoking it.
- **`gh auth` needs the `workflow` scope** to push `.github/workflows/*` (`gh auth refresh -h github.com -s workflow`).
- Until `@studio/core` is on npm, each client repo carries a packed tarball in `vendor/`; `pnpm upgrade-client <slug> vendor` refreshes it.

## One-time setup order

1. Designer creates the accounts above and invites the developer.
2. Register `<studio-domain>` (or move its DNS) at Cloudflare; verify it at Resend; create R2 bucket `studio-media` with custom domain `media.<studio-domain>`.
3. Install the Vercel GitHub App on the org (all repositories, so new client repos are covered).
4. Create Neon project `studio` (Launch); note pooled connection string.
5. Designer runs `claude setup-token`; developer adds all secrets to the `studio` repo (Settings → Secrets → Actions).
6. Set a calendar reminder 11 months out: regenerate `CLAUDE_CODE_OAUTH_TOKEN`.

## Go-live checklist per client (beyond add-domain / set-stripe / set-admins)

- Verify the site in **Google Search Console** (DNS TXT on the client's domain) and submit the sitemap.
- Create/claim the **Google Business Profile** and point it at the new site (the JSON-LD is already there).
- If replacing an old site, collect its important URLs and ask the developer for **redirects** (a small `next.config` list) so links and rankings survive.
- Traffic numbers: **Vercel Web Analytics** is on by default (cookieless); read it in the Vercel dashboard per project.
- Bookings/appointments (gyms, salons, consultants): link out to Calendly/Square from a CTA — the site deliberately doesn't reimplement scheduling.

## Rotating a token without the developer

Regenerate it at the service, paste it into GitHub → `studio` → Settings → Secrets → Actions (and, if the table above says so, the intake app's Vercel env → redeploy). Nothing else references it.
