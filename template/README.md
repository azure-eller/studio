# Client site

Built by the studio pipeline from `brief.json`. Read `CLAUDE.md` before changing anything.

- `pnpm scaffold` regenerates the protected files from the brief.
- `pnpm dev` runs locally (copy `.env.example` → `.env.local`).
- `pnpm check:site` is the gate every change must pass.
